<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="outsideAffiliation">

    <resultMap id="result" type="OutsideAffiliation" extends="affiliation.result">
    </resultMap>

    <sql id="baseSelect">
        select <include refid="affiliation.cols"/>
          from affiliation
         where is_cost_center = 'N'
    </sql>

    <select id="getById" parameterType="Integer" resultMap="result">
        <include refid="baseSelect"/>
         and affiliation_id = #{value,jdbcType=NUMERIC}
    </select>

    <select id="getByMap" resultMap="result">
        <include refid="baseSelect"/>
        <include refid="affiliation.filters"/>
    </select>

</mapper>
