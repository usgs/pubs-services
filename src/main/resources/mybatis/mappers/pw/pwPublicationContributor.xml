<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 

<mapper namespace="pwPublicationContributor">

    <sql id="baseSelect">
        select <include refid="publicationContributor.cols"/>
          from publication_contributor
               join contributor
                 on publication_contributor.contributor_id = contributor.contributor_id
    </sql>

    <select id="getByPublication" parameterType="Integer" resultMap="publicationContributor.result">
        <include refid="baseSelect"/>
        where publication_id = #{value,jdbcType=NUMERIC}
           order by contributor_type_id, rank
    </select>

</mapper>
